# Magic The Gathering card collection explorer
By Kristian Sandström - kristian@sandweb.se

To run:
1. `cd demo_env && pipenv shell`
2. `python backend/manage.py runserver`
3. (new terminal) `cd frontend && npm install && npm start`

